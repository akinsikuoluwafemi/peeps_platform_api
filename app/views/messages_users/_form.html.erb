<%= form_with(model: messages_user, local: true) do |form| %>
  <% if messages_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(messages_user.errors.count, "error") %> prohibited this messages_user from being saved:</h2>

      <ul>
        <% messages_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
