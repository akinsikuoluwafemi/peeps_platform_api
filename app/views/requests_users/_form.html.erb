<%= form_with(model: requests_user, local: true) do |form| %>
  <% if requests_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(requests_user.errors.count, "error") %> prohibited this requests_user from being saved:</h2>

      <ul>
        <% requests_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
